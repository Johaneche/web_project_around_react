import PopUp from "./PopUp.js";

export default class PopupWithForm extends PopUp {
  // constructor(parameters) {
  // Lleva un callback del envío del formulario al constructor, así como el selector popup.
  // }

  _getInputValues() {
    // Almacena un método privado llamado _getInputValues(), que recopila datos de todos los
    // campos de entrada
  }

  // setEventListeners() {
  // Modifica el método padre setEventListeners(). El método setEventListeners() de la clase
  // PopupWithForm debe agregar al formulario un controlador de eventos submit y el detector
  // de eventos click en el icono para cerrar.
  // }

  // Modifica el método padre close() para reiniciar el formulario una vez se cierre el popup

  // Crea una instancia de la clase PopupWithForm para cada popup.
}
